@model MvcTemplate.Web.ViewModels.Comments.CommentAddViewModel

@using (Ajax.BeginForm("Add", "Comments", new { area = "", irregularityId = int.Parse(Url.RequestContext.RouteData.Values["id"].ToString()) }, new AjaxOptions
{
    UpdateTargetId = "comments",
    InsertionMode = InsertionMode.Replace,
    HttpMethod = "POST"
}, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        @Html.TextAreaFor(m => m.Content, new { @class = "form-control", placeholder = "Add a comment...", required = "required", rows = "4" })
    </div>
    <div class="form-group">
        <input type="submit" class="btn btn-submit" value="Post comment" />
    </div>
}